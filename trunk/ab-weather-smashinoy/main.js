/**Провайдер AnyBalance (http://any-balance-providers.googlecode.com)*/function main() {	AnyBalance.setDefaultCharset('windows-1251');	var prefs = AnyBalance.getPreferences();	AnyBalance.trace('Connecting...');	checkEmpty(prefs.city, 'Введите город!');		var info = AnyBalance.requestPost('http://ab1.smashinoy.ru/get_xml/', {		city: prefs.city	});	if (/<city><\/city>/i.test(info))		throw new AnyBalance.Error('Не удалось получить данные. Сайт изменен?');			var result = {success: true};		var balance = getParam(info, null, null, /<score>\s*\d+/i, replaceTagsAndSpaces, parseBalance);	var text = getParam(info, null, null, /<txt>([\s\S]*?)<\/txt>/i, replaceTagsAndSpaces, html_entity_decode);	if(balance == 0 || balance == 1) {		getParam(balance, result, 'score');		getParam(text, result, 'txt');	} else if(balance >= 2 &&  balance <= 6) {		getParam(balance, result, 'score');		getParam(balance+' '+text, result, 'txt');	} else {		getParam(balance, result, 'score');		getParam(text, result, 'txt');			}		getParam(info, result, '__tariff', /<city>([\s\S]*?)<\/city>/i, replaceTagsAndSpaces, html_entity_decode);	getParam(info, result, 'prov_city', /<city>([\s\S]*?)<\/city>/i, replaceTagsAndSpaces, html_entity_decode);		AnyBalance.setResult(result);}